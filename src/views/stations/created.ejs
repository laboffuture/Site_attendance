<%- include('../partials/app-top', { title: title, active: active }) %>

<div class="oh-page-header">
  <h1 class="oh-page-title">Station registered</h1>
  <p class="oh-page-subtitle"><%= stationName %> · <%= siteLabel %></p>
</div>

<div class="oh-card oh-form-card">
  <div class="oh-alert oh-alert--warning">
    Copy this station key now — it is shown only once and cannot be recovered.
    Generate a new one by re-registering if it is lost.
  </div>
  <label class="oh-label">Station key</label>
  <pre class="oh-keybox"><%= stationKey %></pre>
  <p class="oh-muted">
    On the site laptop, open <code>/station/login</code> and paste this key. The
    laptop then captures attendance for <strong><%= siteLabel %></strong> only.
  </p>
  <div class="oh-form-actions">
    <a class="oh-btn oh-btn--secondary" href="/stations">Done</a>
  </div>
</div>

<%- include('../partials/app-bottom') %>
