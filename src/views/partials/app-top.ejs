<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('head', { title: title }) %>
  </head>
  <body>
    <div class="oh-wrapper">
      <%- include('sidebar', { active: active }) %>
      <div class="oh-main">
        <%- include('topbar') %>
        <main class="oh-content">
          <% if (flash) { %>
          <div class="oh-alert oh-alert--<%= flash.type %>"><%= flash.text %></div>
          <% } %>